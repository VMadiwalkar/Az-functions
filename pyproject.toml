[project]
name = "azure-functions-fastapi"
version = "0.1.0"
description = "FastAPI app hosted on Azure Functions Flex Consumption"
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
  "azure-functions>=1.23.0",
  "fastapi>=0.115.0,<1.0.0",
]

[tool.uv]
package = false
